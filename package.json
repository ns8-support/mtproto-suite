{
  "name": "mtproto-suite",
  "version": "2.0.0",
  "description": "MTProto panel + service node — unified project",
  "private": true,
  "scripts": {
    "build": "for d in shared service-node panel-backend panel-frontend; do echo \"Building $d...\"; (cd $d && npm run build); done",
    "build:shared": "cd shared && npm run build",
    "build:node": "cd service-node && npm run build",
    "build:backend": "cd panel-backend && npm run build",
    "build:frontend": "cd panel-frontend && npm run build",
    "lint": "for d in shared service-node panel-backend; do (cd $d && npm run lint); done",
    "clean": "rm -rf */dist */node_modules node_modules"
  },
  "engines": {
    "node": ">=20"
  }
}
